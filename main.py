from guiApp import guiApp
#init population


#pop = population(chromosome_list,population_size,route_size,crossover_probability, mutation_probability)
#for i in range(0,300):
#    pop.calcFitness()
#    print(pop.pop)
#    pop.tournamentSelection()
    #print("FAZA: SELEKCJA", len(pop.pop),len(pop.choosen),len(pop.notChoosen))
    #print (len(pop.notChoosen))
    #print (pop.choosen)
    #print (pop.notChoosen)
    #print (len(pop.choosen))
#    pop.crossover()
    #print("FAZA: CROSSOVER", len(pop.pop))
   # pop.calcFitness()

    #print ("")
    #print(pop.pop)
    #print ("FAZA: SELEKCJA",len(pop.pop))
    #print (len(pop.pop))



gui = guiApp()
gui.run()
